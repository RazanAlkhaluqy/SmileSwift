<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmileSwift"
        tools:targetApi="31">

        <activity
            android:name=".Register"
            android:exported="false" />
        <activity
            android:name=".LogIn"
            android:exported="true"> <!-- Make sure it's exported if it's meant to be accessible externally -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ViewDoctors"
            android:exported="false" />
        <activity
            android:name=".ViewReservations"
            android:exported="false" />
        <activity
            android:name=".Available"
            android:exported="false" />
        <activity
            android:name=".DeleteSchedule"
            android:exported="false" />
        <activity
            android:name=".Add"
            android:exported="false" />
        <activity
            android:name=".LogOut"
            android:exported="false" />
        <activity
            android:name=".UpdateAppointmentActivity"
            android:exported="false" />
    </application>

</manifest>
